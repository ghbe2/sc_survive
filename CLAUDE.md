@AGENTS.md

# Claude Code 向け補足
- 現行仕様・規約・作業ルールはすべて AGENTS.md（上に読み込み済み）が正。
- ローカル動作確認: NestCam/.claude/launch.json の "sugarsurvive"（PowerShell製 静的サーバー, port 8123）。なければこのフォルダで任意の静的サーバーでも可。
- 実装後は push まで行う（GitHub Pages がスマホ実機テスト環境。オーナーはスマホでリロードして確認する）。
- コミットは日本語の要約1行で。
